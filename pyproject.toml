[project]
name = "kapital-pricer"
version = "0.1.0"
description = "yfinance price microservice for the portfolio dashboard"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "requests>=2.34.2",
    "uvicorn>=0.47.0",
    "yfinance>=1.3.0",
]
